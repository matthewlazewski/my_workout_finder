<h1>All Categories</h1>
<ul>
    <%= @categories.each do |c| %>
        <li><%= link_to c.name, category_path(c) %> - <%= c.description %></li>
    <% end %>
</ul>

<%=link_to 'Add a new category', new_category_path %>